import React from 'react';
import styled from 'styled-components';

const PageRoom = () => {
  return <StyledWrapper>객실 페이지</StyledWrapper>;
};

export default PageRoom;

const StyledWrapper = styled.div`
  width: 100%;
`;
